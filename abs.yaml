# bun is not on pkgs/main yet (AnacondaRecipes/bun-feedstock#1). Resolve it
# from that PR's staging channel (commit 17b6959: includes the macOS
# `bun build --compile` fix); remove this file once bun is on main.
channels:
  - https://staging.continuum.io/pbp/fs/bun-feedstock/pr1/17b6959
